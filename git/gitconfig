; global settings
[user]
    name = Mohammed Fareed
    email = mohdf.fareed@icloud.com
	signingkey = /Users/mohdfareed/.ssh/id_ed25519.pub
[commit]
    gpgsign = true ; sign commits using GPG key
[gpg]
	format = ssh
[init]
    defaultBranch = main
[fetch]
    prune = true ; remove remote-tracking branches for deleted remote branches
[pull]
    rebase = true ; rebase local commits on top of the updated upstream branch

; terminal settings
[status]
    short = true ; git status uses --short option by default
[color]
    ui = auto

; vscode settings
[core]
    editor = code --wait
[diff]
    tool = vscode
[difftool "vscode"]
    cmd = code --wait --diff $LOCAL $REMOTE
[merge]
    tool = vscode
[mergetool "vscode"]
    cmd = code --wait $MERGED

; git aliases
[alias]
    graph = log --graph --abbrev-commit

    graphc = log --graph --format=format:'%C(bold blue)%h%C(reset) - \
%s %C(dim white)- %an%C(reset)%C(auto)%d%C(reset)'
