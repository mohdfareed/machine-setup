[user]
    name = Mohammed Fareed
    email = mohdf.fareed@icloud.com
    signingkey = 45752BF553D65F05
    ; RIT email = mff9108@rit.edu
    ; RIT signingkey = 7914CE516B7DF7FE
[init]
    defaultBranch = main
[commit]
    gpgsign = true
[core]
    editor = code --wait
[color]
    ui = auto
[diff]
    tool = vscode
[difftool "vscode"]
    cmd = code --wait --diff $LOCAL $REMOTE
[merge]
    tool = vscode
[mergetool "vscode"]
    cmd = code --wait $MERGED

[alias]
    graph = log --graph --abbrev-commit
    graphcompact = log --graph --oneline
