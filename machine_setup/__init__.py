"""Library of modules containing `setup()` methods used for setting up a new
machine. These modules are imported and used by the `setup.py` script.
"""

from . import git, homebrew, macos, python, zsh
