# theme
ZSH_THEME="common"

# plugins
plugins=(
    colored-man-pages # adds colors to man pages
    nvm # automatically sources and adds autocompletions for nvm
    osx # provides a few utilities on macOS
)

# load aliases and functions
source $ZDOTDIR/.aliases

# python
# add unversioned symlinks to path
export PATH="$(brew --prefix)/opt/python/libexec/bin:$PATH"

# ruby
export RBENV_ROOT="$DEVELOPER/rbenv" # rbenv directory
eval "$(rbenv init -)" # evaluate rbenv

# nvm
export NVM_DIR="$DEVELOPER/nvm" # nvm directory
export NVM_LAZY=1 # let oh-my-zsh lazy evaluate nvm

# load oh-my-zsh
source $ZSH/oh-my-zsh.sh
# load zsh syntax highlighting
source $(brew --prefix)/share/zsh-syntax-highlighting\
/zsh-syntax-highlighting.zsh
